{
  "scripts": {
    "build": "npm install && install --prefix frontend-tracker && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "react-hot-toast": "^2.4.1"
  }
}
